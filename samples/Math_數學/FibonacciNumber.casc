func fib(in: number): number {
    if in == 0 || in == 1
        return in
    return fib(in - 1) + fib(in - 2)
}

print("Please Enter a number: ")
var in = input()
var result = fib(number(in))
print(string(result))