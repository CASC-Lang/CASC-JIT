func fab(in: number): number {
    if in == 0 || in == 1
        return in
    return fab(in - 1) + fab(in - 2)
}

print("Please Enter a number: ")
var in = input()
var result = fab(number(in))
print(string(result))